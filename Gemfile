source 'https://rubygems.org'

git_source(:github) do |repo_name|
  repo_name = "#{repo_name}/#{repo_name}" unless repo_name.include?("/")
  "https://github.com/#{repo_name}.git"
end

# Faker para criar itens automatizados e aleatorios
gem 'faker'
gem 'rails', '~> 5.1'
gem 'sqlite3'
gem 'jquery-rails'
gem 'turbolinks'
gem 'twitter-bootstrap-rails'
gem 'puma'
gem 'tzinfo-data'
# a gem sass e coffee-script foram utilizadas para o uso do twitter bootstrap
gem 'sass', '~> 3.4', '>= 3.4.22'
gem 'coffee-script', '~> 2.4', '>= 2.4.1'
